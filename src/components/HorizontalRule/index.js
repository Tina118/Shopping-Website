import {Flex} from 'rebass';
import styled from 'styled-components';

/*
    HorizontalRule
    Draw horizontal Line
*/

const HorizontalRule = styled(Flex)`
    border-bottom: 2px solid #E0E0E0;
    margin-top:15px;
    margin-bottom:15px;
`

export default HorizontalRule;